package lordofobjects;

public class Male extends MainCharacter {
	int agility;

	public Male(int health, int attack, String name, int agility) {

		super(health, attack, name);
		this.agility = agility;
	}
	
@Override
	
	public int attack(int type) {
	int damage;
		if (type == 1) {
			damage = 5;
			double probability = 0.8;
		}
		else if (type == 2) {
			int damage = 10;
			double probability = 0.6;
		}
		else if (type == 3) {
			double odds = Math.random();
			if (odds >= 0.5) {
				System.out.println("You've blinded the manbeast permanently, he's very upset, but otherwise useless");
				int damage = 100;
				return damage;
			}
		}
		int damage = 3 + ((int)(Math.random() * 3));
		double probability = Math.random();
		if (probability >= 0.5) {
			return damage;
		}
		else {
			damage = 0;
			return damage;
		}
	}
}
